# Supabase — source of truth (spec §3)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Shared event PIN gating /staff and /dashboard (spec §3).
# Held by the staff lead. Never printed on a tag, poster or anything
# visitor-facing. Rotate after the event.
EVENT_PIN=
# Signing secret for the short-lived staff cookie.
EVENT_SESSION_SECRET=
# Local date the event PIN cookie expires at, end of event day (YYYY-MM-DD).
EVENT_END_DATE=
